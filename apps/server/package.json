{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch --clear-screen=false src/index.ts",
    "lint": "eslint --ext .ts",
    "build": "tsc --build"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
